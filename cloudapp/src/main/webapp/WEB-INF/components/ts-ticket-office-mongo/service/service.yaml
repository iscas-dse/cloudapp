apiVersion: v1
kind: Service
metadata:
  name: ts-ticket-office-mongo
  labels:
    app: ts-ticket-office-mongo
spec:
  ports:
    - port: 27017
      name: mongo
  selector:
    app: ts-ticket-office-mongo