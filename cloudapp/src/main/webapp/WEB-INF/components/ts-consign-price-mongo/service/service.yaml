apiVersion: v1
kind: Service
metadata:
  name: ts-consign-price-mongo
  labels:
    app: ts-consign-price-mongo
spec:
  ports:
    - port: 27017
      name: mongo
  selector:
    app: ts-consign-price-mongo