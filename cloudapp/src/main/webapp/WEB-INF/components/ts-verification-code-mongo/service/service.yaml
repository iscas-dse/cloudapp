apiVersion: v1
kind: Service
metadata:
  name: ts-verification-code-mongo
  labels:
    app: ts-verification-code-mongo
spec:
  ports:
    - port: 27017
      name: mongo
  selector:
    app: ts-verification-code-mongo