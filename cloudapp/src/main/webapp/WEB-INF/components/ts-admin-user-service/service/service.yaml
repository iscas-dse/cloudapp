apiVersion: v1
kind: Service
metadata:
  name: ts-admin-user-service
  labels:
    app: ts-admin-user-service
spec:
  ports:
    - port: 16115
      name: http
  selector:
    app: ts-admin-user-service